export enum CategoryEnum {
  products = "products",
  stands = "stands",
  flowers = "flowers",
}
export enum OrderStatusEnum {
  success = "success",
  returned = "returned",
  cancelled = "cancelled",
}
